import FirstHero from './first/FirstHero'
import SecondHero from './second/SecondHero';

const Hero = () => {
  const type = "firstHero";
  if ( type === "firstHero") {
  return (
    <SecondHero />
  )}
}

export default Hero