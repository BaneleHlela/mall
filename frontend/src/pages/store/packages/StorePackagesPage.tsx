import FirstStorePackagesPage from "./first/FirstStorePackagesPage";

const StorePackagesPage = () => {
  const type = "first"; 
  if (type === "first") {
  return (
    <FirstStorePackagesPage />
  )
  }
}

export default StorePackagesPage;